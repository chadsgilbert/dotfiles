# PATH
if [ -d $HOME/bin ]; then
    PATH=$PATH:$HOME/bin:$HOME/.local/bin
fi

# LD_LIBRARY PATH (for C++ shared objects).
LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/lib

export PATH
export LD_LIBRARY_PATH
