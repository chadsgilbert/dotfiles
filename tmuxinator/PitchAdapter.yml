# ~/.tmuxinator/PitchAdapter.yml

name: PitchAdapter
root: ~/projects/rti-io-headers

windows:
  - git:
      layout: main-vertical
      panes:
          - git status
          - git tree
  - editor:
      layout: main-vertical
      panes:
        - vim
        - cd ~/workspace/rti-io-headers/Debug
